/** V1 Discovery hero brand visual — editorial photo, not event content. */
export function DiscoveryBrandVisual() {
  return (
    <div className="discovery-brand-visual" aria-hidden>
      <img
        className="discovery-brand-visual-art"
        src="/discovery-hero.png"
        alt=""
        width={571}
        height={571}
        decoding="async"
      />
    </div>
  )
}
