# Weekly Bay Area regional / destination events for Admin review.
# Thursdays 8:00 AM Pacific: re-ingest watchlist drive rows + regional-leads-inbox.json + report.
# Same morning as core-city weekend ingest. Web/social (~1 hour) + 小紅書 search stays
# human/agent-assisted — paste official URLs into the inbox.

name: Discover regional (weekly)

on:
  schedule:
    # Thursdays 15:00 UTC ≈ 8:00 AM Pacific (PDT); 7:00 AM during PST
    - cron: '0 15 * * 4'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: discover-regional-weekly
  cancel-in-progress: true

jobs:
  regional:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Regional weekly pass
        run: npm run discover:regional-weekly

      - name: Reminder — weekend Worth a Drive web & social (~1 hour)
        if: github.event_name == 'schedule'
        run: |
          echo "::notice title=Weekend Worth a Drive search::This weekend + next, within ~1 hour of Palo Alto / Los Altos / Mountain View / Sunnyvale: pumpkin patches, apple days, harvest festivals. Follow docs/regional-weekend-drive-search.md. Paste official URLs into data/discovery/regional-leads-inbox.json, then re-run this workflow or npm run discover:regional-weekly."

      - name: Reminder — 小紅書 lead pass
        if: github.event_name == 'schedule'
        run: |
          echo "::notice title=小紅書 regional leads::Search Bay Area parent roundups on 小紅書. Add official event URLs to data/discovery/regional-leads-inbox.json, then re-run this workflow or discover:regional-weekly locally."

      - name: Verify build
        run: npm run build

      - name: Commit regional discovery updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/data/discovery-candidates.json data/discovery/regional-leads-inbox.json data/discovery/regional-weekly-*.md data/calendar-watchlist.json
          if git diff --staged --quiet; then
            echo "No regional discovery changes — skipping commit."
          else
            git commit -m "chore: weekly regional discovery pass"
            git push
          fi
